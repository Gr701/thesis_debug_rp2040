\section{Metrics description}

To compare the approaches explored in this work we will have to define a set of metrics, both quantative and qualitative. 

\subsection{Quantative metrics}

The first metric which we will consider while comparing the different approaches is the price of hardware.

time to produce
physical size
"lines of code" or RAM taken

Performance and Overhead Metrics

These metrics measure the impact of the debugger on the running application. 

    Debug Overhead: The percentage of CPU, memory, or time added by the debugger. Heavy breakpoints or conditional logging can significantly increase execution time.
    Slowdown Factor: Often measured in production debugging, this is the ratio of execution time with the debugger active vs. inactive (e.g., a "10x–100x slowdown" in intensive debugging scenarios).
    Memory Access Speed: The speed at which a debugger reads/writes data to the target system, crucial for embedded processors.
    Breakpoint Latency: Time taken to halt execution when a breakpoint is hit. 

\subsection{Qualitative metrics}

maintenance of the technologies used 
beginner friendly usage
comfort of use 
begginer friendly installation 
limitations of the approach


\section{Apply metrics}
\section{Comparison of probes}
